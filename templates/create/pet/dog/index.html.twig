{% extends 'content.base.twig' %}

{% block title %}Add new Dog to your Family{% endblock %}

{% block content_body %}


<div class="example-wrapper">
    {{ form_start(dogForm)}}
        {{ form_widget(dogForm) }}

        <button class="btn btn-primary" type="submit">Create!</button>
    {{ form_end(dogForm)}}
</div>
{% endblock %}
